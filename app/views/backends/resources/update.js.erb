$(document).ready(function(){
  $("#<%= current_object.class.name %><%= current_object.id %>").replaceWith('<%= escape_javascript(render(partial: "show",locals:{current_object: current_object})) %>');
  <% if @message %>
  toastr.success("<%= @message %>")
  <% end %>
});