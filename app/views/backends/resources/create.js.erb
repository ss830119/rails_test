$(document).ready(function(){
  $("#<%= current_object.class.name.tableize %>").append('<%= escape_javascript(render(partial: "show",locals:{current_object: current_object})) %>');
  <% if @message %>
  toastr.success("<%= @message %>")
  <% end %>
});